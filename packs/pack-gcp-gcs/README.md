# Pack stub: GCP / GCS

Plantilla para cuando exista `@afn-ecosystem/mcp-gcp-deploy` (o MCP equivalente) con kind `gcs-static`.

Sin secretos. El orquestador AFN no necesita rebuild si el MCP declara `cloudDeliveryRole: deploy` y tools canónicas.
