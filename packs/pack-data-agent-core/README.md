# Pack — Data Agent core

MCP genérico + skill core + agente WhatsApp.  
Dominios (domicilios, campañas, …) = packs `pack-domain-*` o skills adicionales.

Implementación del servidor: `notions/packages/afn-mcp-data-agent` (npm `@afn-ecosystem/mcp-data-agent`).

Despliegue Lightsail: ver `deploy/` en ese paquete (nginx + systemd + env).
